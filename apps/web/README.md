# Web App

## Run

```bash
npm install
npm run dev
```

The app expects the API at `http://localhost:8000` by default.

Create `apps/web/.env` from `.env.example` only if you need to override `VITE_API_BASE_URL`.
